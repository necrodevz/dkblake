<%= form_with(model: skill, local: true) do |form| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :skill_name %>
  </div>

  <div class="field">
    <%= form.label :skilliq %>
    <%= form.number_field :skilliq, id: :skill_skilliq %>
  </div>

  <div class="field">
    <%= form.label :me %>
    <%= form.text_field :me, id: :skill_me %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
